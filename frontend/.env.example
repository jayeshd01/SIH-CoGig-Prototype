# Sahyog / CoGig Frontend Environment Configuration

# Backend API Endpoint
# Default: /api (proxied automatically to http://127.0.0.1:3000 by Vite dev/preview server)
# In production or standalone setup, set to the backend API URL:
VITE_API_URL=/api
